package gpa;

import java.util.Scanner;

public class CalculateGPA {
	String name;
	int id;
	
	public CalculateGPA(String name, int id) {
		this.name = name;
		this.id = id;
	}

	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);
		System.out.println("Enter your name: ");
		String name = scanner.nextLine();
		
		System.out.println("Enter your id:(integer) ");
		int id = scanner.nextInt();
		
		
		CalculateGPA imranGPA = new CalculateGPA(name,id);
		
		
		
		System.out.println("Enter your attendace mark:  ");
		int attendaceMark = scanner.nextInt();
		
		System.out.println("Enter your CT mark:  ");
		int ctMark = scanner.nextInt();
		
		System.out.println("Enter your assignment mark:  ");
		int assignmentMark = scanner.nextInt();
		
		
		
		System.out.println("Enter your final mark:  ");
		int finalMark = scanner.nextInt();
		
		double totalPercentage = ((attendaceMark + ctMark + assignmentMark + finalMark)/100)*100;
		
		
		String gpa = imranGPA.getGPA((int)totalPercentage);
		System.out.println(gpa);

	}

	
	public String getGPA(int marks) {
		if (marks >= 80) {
			return "A+";
		} else if (marks >= 75) {
			return "A";
		} else if (marks >= 70) {
			return "A-";
		} else if (marks >= 65) {
			return "B+";
		} else if (marks >= 60) {
			return "B";
		} else if (marks >= 55) {
			return "B-";
		} else if (marks >= 50) {
			return "C+";
		} else if (marks >= 45) {
			return "C";
		} else if (marks >= 40) {
			return "D";
		} else {
			return "F";
		}
	}
}
